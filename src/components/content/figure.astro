---
import Img from "@/components/content/img.astro";

interface Props {
	alt?: string;
	src: string;
}

const { alt = "", src } = Astro.props;
---

<figure>
	<Img alt={alt} src={src} />
	{
		Astro.slots.has("default") ? (
			<figcaption>
				<slot />
			</figcaption>
		) : null
	}
</figure>
